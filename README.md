# discord-bot
--------------
Para usar el bot es necesario tener los módulos de node necesarios que se muestran en package.json.

Es necesario tener un archivo ".env" en donde tengas guardadas las claves y los tokens. IMPORTANTE: Ese archivo nunca se puede guardar en un sitio público, porque te pueden capturar el bot y inutilizarlo.
